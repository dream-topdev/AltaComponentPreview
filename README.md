# Instructions

This project was bootstrapped with [Create React App](https://github.com/facebook/create-react-app).

You can run the included server by installing dependencies with `npm install`, then `npm run start`.

The component you'll need to implement is `src/ComponentPreview.tsx`. For more info, see the project instructions you received by email.
